import React from 'react';
import { AppImage, AppView } from '../common';
const logo = require('../assets/imgs/logo1.png');

const Logo = () => {
    return (
        <AppImage equalSize={45} source={logo} />
    );
};

export default Logo
    ;
