import {StyleSheet, Text, View, Image} from 'react-native';
import React from 'react';

import bgImage from '../../assets/imgs/topBarBg.png';

const TopBarBg = () => {
  return <Image source={bgImage} />;
};

export default TopBarBg;

const styles = StyleSheet.create({});
